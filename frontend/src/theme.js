import {createTheme} from '@mui/material/styles';

export default createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#0066a1',
      contrastText: '#fff'
    },
    secondary: {
      main: '#cc0000',
    },
  },
});